<!DOCTYPE html>
<html>
	 <head>
	   <title>Simple Cms  <%= " | #{@page_title}" if @page_title %></title>
	  <%= stylesheet_link_tag('application', :media => 'all') %>
	  <%= javascript_include_tag('application') %>
	 </head>
	 <body>
        <nav class="navbar navbar-default navbar-static-top nav-color" style="margin-bottom: 0">
		    <div class="navbar-header col-lg-offset-5 ">
	            <h3 style="color:white">SIMPLE CMS</h3>
		    </div>
		    <div class="home-and-logout">
			    <%= link_to("Home", {:controller => "public", :action => 'index'}, :class => "main") %>
		    </div> 

		</nav>
		<%= yield %> 
      
		<% if !flash[:notice].blank? %>
		     <br/>
		     <div class="alert alert-info" id="alert"> <%= flash[:notice]%></div>
		<% end %>
	</body>
	<div class="panel-footer navbar-fixed-bottom text-center"> @ All copies are reserved at clickapps company</div>
</html>