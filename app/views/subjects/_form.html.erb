<table class="table">
   <tr>
      <td ><b><%= f.label(:name)%> :</b></td><td><%= f.text_field(:name) %></td>
   </tr>
   <tr>
      <td ><b><%= f.label(:position)%> :</b></td><td><%= f.select(:position, 1..@subject_count) %></td>
   </tr>
   <tr>
     <td><b><%= f.label(:visible)%> :</b></td><td><%= f.check_box(:visible) %></td>
   </tr>
</table> 