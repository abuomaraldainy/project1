<% @page_title = "Delete Page" %>
<% @panel_header = "Delete Page" %> 
<div class="row"> 
    <div class="col-lg-12">
      <%= link_to("<<< Go To List", {:action => 'index',:subject_id => @subject.id }, :class => "") %> 
    </div>
</div>   
<div class="row">
   <div class="col-lg-4"> </div>
   <div class="col-lg-4">
		 <%= form_for(:section, :url => {:action => 'destroy', :id => @page.id,:subject_id => @subject.id }) do |f| %>
		    <div class="alert alert-danger">
		        <p class="text-center"> Are you sure you want to delete <br/> <%=">> #{@page.name} << " %>?</p>
		    </div>
		    <div class="col-sm-offset-0"> 
		    	<%= submit_tag('Delete Page',:class => 'btn btn-danger')%> 
		 	</div>
		<% end %>      
   </div>
   <div class="col-lg-4"> </div>
</div> 

